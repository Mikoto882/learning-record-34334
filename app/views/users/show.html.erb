<div class="my-page">
  <div class="my-page-title">
    マイページ
  </div>

  <div class="profile-group">
    <ul class="profile-list">
      <li class="profile-nickname">Tom</li>
      <li class="list-group">こんにちは、Tomです。得意教科は英語。苦手教科は数学です。大学受験に向けて精進中です！</li>
      <li class="list-group">高校3年生</li>
    </ul>
    <div class="profile-edit-btn">
      <%= link_to "ユーザー情報を変更する", edit_user_profile(@profile.id), method: :get %>
    </div>
  </div>

  <div class="my-page-records">
    <div class="user-show-nickname">
      <%= "#{@user.nickname}さんの記録一覧" %>
    </div>
    <% @learns.each do |learn| %>
      <div class="record">
        <div class="record-top">
          <div class="record-title">
            <%= link_to learn.title, learn_path(learn.id), method: :get %>
          </div>
          <div class="record-subject">
            <%= learn.subject.name %>
          </div>
          <div class="record-date">
            <%= learn.created_at %>
          </div>
        </div>
        <div class="record-bottom">
          <div class="record-text">
            <%= learn.description %>
          </div>
          <%= image_tag learn.image.variant(resize: '500x500'), class: 'learn-image' if learn.image.attached? %>
        </div>
      </div>
    <% end %>
  </div>
</div>
